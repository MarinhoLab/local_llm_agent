# .env.example
# Copy to .env and edit:
#   cp .env.example .env
# All other defaults are in compose.yml.

# SSH tunnel to DGX Spark
DGX_SSH_TARGET=USER@DGX_SPARK_IP
DGX_REMOTE_HOST=localhost
DGX_REMOTE_PORT=8000
LOCAL_TUNNEL_PORT=8000
SSH_DIR=${HOME}/.ssh
SSH_EXTRA_OPTS=

