X1="${BASH_SOURCE[0]}"
X2="${OPENCODE_CONFIG_DIR:=${HOME}/.config/opencode}"
X3="${OPENCODE_BASE_URL%/}"
X4="${OPENCODE_API_KEY:=""}"
X5="${OPENCODE_PROVIDER_NAME:=DGX Spark vLLM}"
X6="${OPENCODE_CONTEXT_LENGTH:=262144}"
X7="${OPENCODE_MODEL_ID:=qwen-local}"
X8="${OPENCODE_PROVIDER_ID:=dgx-vllm}"
X9="${OPENCODE_BASE_URL:=http://127.0.0.1:8000/v1}"
X10="${OPENCODE_OUTPUT_LENGTH:=16384}"
X11="${1:-qwen-local}"
X12="${2:-qwen-local}"
X13="${1:-OPENCODE_VLLM_OK}"
X14="${1:-$(pwd)}"
X15="${HOME}"
X16="${CONFIG_FILE}"
X17="${PATH}"
X18="unknown"
X19=""
X20="8000"
